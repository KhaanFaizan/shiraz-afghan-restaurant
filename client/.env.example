# ── Shiraz Frontend Environment Variables ──
# In development, leave VITE_API_URL unset.
# Vite proxies /api → http://localhost:5000 automatically (see vite.config.js).
#
# In production, set this to your deployed backend base URL:
# VITE_API_URL=https://api.shirazrestaurant.co.uk/api
